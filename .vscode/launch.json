{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "go build mastosync",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/main.go",
            "args": [
                "mandala"
            ],
            "cwd": "${userHome}/Desktop"
        }
    ]
}
